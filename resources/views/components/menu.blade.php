
  <section id="menu" class="buttons-left">
    <x-button href="{{route('vender')}}">Iniciar Vendas</x-button>
    <x-button href="{{route('home')}}">Área de Produtos</x-button>
    <x-button href="{{route('home')}}">Área de Clientes</x-button>
    <x-button href="{{route('home')}}">Minha Conta</x-button>
    <x-button href="{{route('logout')}}">Sair do Sistema</x-button>
  </section>
